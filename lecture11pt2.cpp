#include<iostream>
using namespace std;
int main(){
     long long int a;
    int b;
    float c;
    cout << "The size of int is :"<<sizeof(b)<<"The size of long long bit is "<<sizeof(a);
    cout << "the address of a is "<<&a <<"the address of b is "<<&b;
    cout << "the address of c is "<<&c;
    return 0;
}